#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel/bin/deprecated/6to5-runtime" "$@"
  ret=$?
else 
  node  "$basedir/../babel/bin/deprecated/6to5-runtime" "$@"
  ret=$?
fi
exit $ret
