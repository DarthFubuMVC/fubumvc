#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel/bin/babel-node" "$@"
  ret=$?
else 
  node  "$basedir/../babel/bin/babel-node" "$@"
  ret=$?
fi
exit $ret
