#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../react-tools/bin/jsx" "$@"
  ret=$?
else 
  node  "$basedir/../react-tools/bin/jsx" "$@"
  ret=$?
fi
exit $ret
